"use client";import {useState}from"react";export default function Login(){const [message,setMessage]=useState("Admin authentication is enabled after database bootstrap.");return <main><section><h1>FinSight365 Admin</h1><p>{message}</p><form onSubmit={async e=>{e.preventDefault();setMessage("Login endpoint will validate password and TOTP after bootstrap.")}}><input placeholder="Email" type="email"/><input placeholder="Password" type="password"/><input placeholder="TOTP code" inputMode="numeric"/><button className="primary">Sign in</button></form></section></main>}
